import React from 'react';
import useRole from '../../../../Hooks/useRole';

const AdminRoute = () => {

    const { role} = useRole()
    return (
        <div>
            
        </div>
    );
};

export default AdminRoute;